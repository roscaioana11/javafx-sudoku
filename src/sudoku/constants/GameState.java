package sudoku.constants;

//tool for modeling data which doesn't change throughout the runtime of the application
//are static final variables and enums
public enum GameState {
    COMPLETE,
    ACTIVE,
    NEW
}
